#include <gtest/gtest.h>
#include "../color_rgb.h"

TEST(color_rgb, constructor) 
{
	EXPECT_EQ(254, 254) << "Unexpected test value.";
}

TEST(color_rgb, public_fields) 
{
    EXPECT_EQ(3, 3) << "Unexpected test value.";
}

TEST(color_rgb, getters_and_setters)
{
    EXPECT_EQ(3, 3) << "Unexpected test value.";
}